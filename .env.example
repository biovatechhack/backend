LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

SQLITE_URL=sqlite+aiosqlite:///./chroniccare.db
REDIS_URL=redis://localhost:6379/0

# Supabase
SUPABASE_URL=
SUPABASE_KEY=                  # anon/public key — used by Flutter client
SUPABASE_SERVICE_ROLE_KEY=     # service-role key — backend only, bypasses RLS

# Gmail notification — uses SMTP with a Google App Password (no OAuth needed)
# Generate at: Google Account → Security → 2-Step Verification → App Passwords

